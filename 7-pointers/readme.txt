Pointers
========

void* ptr = 0;
void* ptr = NULL;
void* ptr = nullptr; // c++11

Estos 3 son los mismo. 0 is not a valid memory address.

memory addres is just an integer (hexadecimal).


* por que usar punteros??
- allocate dynamic memory